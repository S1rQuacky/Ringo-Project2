<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head.ejs") %>
  <a href="/spooktober"><button>HOME</button></a>
  <body>
    <%- include("../partials/header.ejs") %>
    <main>
      <div>
        <article>
          <a href="/spooktober/<%= movie._id %>">
            <h2>
              <%= movie.title %> - <%= moment(movie.released).format('MMM Do YYYY') %><br>
              <div id="poster"> 
                <img src="<%=movie.poster%>" >
            </div>
              <br>
              <%= movie.overview %><br>
              <br>
              My Notes: <%= movie.myNotes %><br>
              My Rating: <%= movie.myRating %>
            </h2>
          </a>
          <a href="/spooktober/<%= movie._id %>/edit"><button>Edit</button></a>
          <form action="/spooktober/<%= movie._id %>?_method=DELETE" method="POST">
            <button>Delete</button>
        </form>
        </article>
      </div>
    </main>
  </body>
</html>